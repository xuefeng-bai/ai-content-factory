# 数据库配置
DATABASE_URL=sqlite:///./data/daily_brief.db

# AI 配置（DashScope/阿里云百炼）
DASHSCOPE_API_KEY=sk-your-api-key
AI_MODEL=qwen-plus

# 跨平台适配
# 所有路径使用 pathlib 处理，不要硬编码路径分隔符
# 所有文件使用 UTF-8 编码
# 命令行使用 python 命令，不要指定版本
